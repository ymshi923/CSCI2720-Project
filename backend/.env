# Example environment variables for the backend
# Copy this file to `backend/.env` and replace placeholder values before running.

# MongoDB connection string (local or Atlas)
MONGODB_URI=mongodb://localhost:27017/cultural-events

# Backend server port
PORT=5000

# Node environment
NODE_ENV=development

# JWT secret for signing tokens (replace with a secure random secret)
JWT_SECRET=d6f6c8v8b8r9w8f9v8=c9d9#

# Frontend URL (for CORS or redirects)
FRONTEND_URL=http://localhost:5173
